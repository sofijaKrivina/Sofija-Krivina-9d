#include <iostream>
using namespace std;

//int main() {
	//int sk1;
	//cin >> sk1;
	//if (sk1 % 2 == 0) { cout << "para"; }
	//else cout << "nepara";


//int main() {
	//int sk1, sk2;
	//cin >> sk1, sk2;
	//if (sk1 >> sk2) { cout << "sk1 ir maksimals"; }
	//else cout << "sk1 is minimals";

int main() {
	Percentage >= 90 % :Grade A;
	Percentage >= 80 % :Grade B;
	Percentage >= 70 % :Grade C;
	Percentage >= 60 % :Grade D;
	Percentage >= 40 % :Grade E;
	Percentage < 40 % :Grade F;
